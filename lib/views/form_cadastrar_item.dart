import 'package:flutter/material.dart';

class FormItem extends StatelessWidget {

  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
          child: const Text('forme')
      ),

    );
  }
}